{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  },
  "ConnectionStrings": {
    "//": "Point this at the same SQLite file the API/summarizer use. Relative paths resolve to the worker's working dir.",
    "Sqlite": "Data Source=../../api/NomadRules.Api/nomadrules.db"
  },
  "Ingest": {
    "//": "Transport mirrors the crawler's createPublisher(): 'local' reads the file queue, 'servicebus' an Azure Service Bus queue.",
    "Transport": "local",
    "LocalQueueDir": "../../crawler/local-queue",
    "ServiceBusConnectionString": "",
    "ServiceBusQueue": "law-changes",
    "MvpDefaultState": "TX",
    "BatchSize": 20
  }
}
