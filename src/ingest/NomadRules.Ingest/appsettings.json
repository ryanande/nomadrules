{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  },
  "ConnectionStrings": {
    "//": "Point this at the same local Postgres the API/summarizer use (see infra/docker-compose.yml).",
    "Postgres": "Host=localhost;Port=5432;Database=nomadrules_dev;Username=nomadrules;Password=nomadrules"
  },
  "Ingest": {
    "//": "Transport mirrors the crawler's createPublisher(): 'local' reads the file queue, 'servicebus' an Azure Service Bus queue.",
    "Transport": "local",
    "LocalQueueDir": "../../crawler/local-queue",
    "ServiceBusConnectionString": "",
    "ServiceBusQueue": "law-changes",
    "MvpDefaultState": "TX",
    "BatchSize": 20
  }
}
