server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback: any path not matching a static asset resolves to index.html.
    location / {
        try_files $uri /index.html;
    }
}
